# npm ci fails ERESOLVE otherwise: agents@0.17.x peer chain conflicts with the zod@3 pin;
# the lockfile was resolved with legacy peer handling (verified 2026-07-15, public CI).
legacy-peer-deps=true
