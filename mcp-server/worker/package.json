{
  "name": "inference-margins-mcp-worker",
  "version": "1.0.0",
  "description": "Cloudflare Workers (remote streamable-HTTP) deployment of the inference-margins MCP server. The shared tool/shape/label sources are copied verbatim from ../src at build time (scripts/build.mjs) \u2014 mcp-server/src stays the single source of truth; only engine.ts (bundler import), reports.ts (live-site fetch) and get_report.ts (async read) are worker-native.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "pretest": "npm run build",
    "test": "node ../../tests/assert-suite-nonempty.mjs test 5 && node --test \"test/*.test.mjs\"",
    "typecheck": "npm run build && tsc -p tsconfig.json",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && tsc -p tsconfig.json && npm test && node scripts/release-gate.mjs --check && node scripts/dcmap-deploy-gate.mjs && wrangler deploy && node scripts/release-gate.mjs --readback",
    "predeploy": "cd ../.. && npm test && npm run test:served-node && npm run test:browser && npm run test:served-browser",
    "release:check": "node scripts/release-gate.mjs --check",
    "release:repin": "node scripts/release-gate.mjs --repin",
    "release:readback": "node scripts/release-gate.mjs --readback",
    "release:dcmap-gate": "node scripts/dcmap-deploy-gate.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "^0.17.3",
    "zod": "^3.25.1",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260629.1",
    "typescript": "^5.6.0",
    "wrangler": "4.135.0"
  }
}
