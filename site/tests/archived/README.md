# Archived traffic baselines

`fixtures-baseline-v211.json` is the immutable v2.1.1 traffic-refactor baseline. IM3 slice 3
archived it for IM6 historical comparison when the rendered engine switched to the v2.2
roofline path. Live traffic-contract parity uses `../fixtures-baseline-v22.json`; this archive
must not be treated as a current-number fixture.
