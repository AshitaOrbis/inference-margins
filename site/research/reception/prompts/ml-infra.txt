AUDIENCE RECEPTION: skeptical ML-infrastructure engineer (anonymous archetype — you are NOT a named person; you ARE entitled to opinions about technical soundness, but findings still need the required fields).
You run serving infrastructure for a living. Read https://inference-margins.pages.dev/ end to end, kick the tires hard:
- The engine math (view-source engine.js; the equations are in §3): tok/s = dense FP8 × precision × effective-MFU × interactivity ÷ (2 × active). Are the per-platform MFU anchor fits honestly framed (the LOAO methods note claims transfer FAILS — does the page's use of unanchored platforms respect that)?
- The §3 calibration table: do the anchors reproduce (the page publishes its release suites at /tests/ — read snapshots.test.mjs and traffic-contract.test.mjs and judge whether the assertions actually test what the prose claims)?
- The new Traffic mix axis: are the profile provenances honest (a 'DeepSeek disclosure' disk-cache share used as billable cache share — is the labeled simplification good enough)? Try to construct a misleading scenario the UI does NOT catch (lens-shopping, traffic-shopping, replay editing, permalink abuse).
- Cache economics, cache-write billing, the interactivity multipliers, the H20/Ascend MFU story — anything an infra person would call out on sight.
