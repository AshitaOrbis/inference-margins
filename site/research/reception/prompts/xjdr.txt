AUDIT: deployment-fidelity — @_xjdr's ncode/Noumena numbers (NOT a position audit; he holds no margin position on the page).
STAGE 1 (FIRST): read the archived sweep https://inference-margins.pages.dev/research/grok-sweep-glm-gb300-plans — the verbatim record of his posts. Extract every number and configuration claim: ~12000 api keys, ~300B tokens, 232/431 tok/s/GPU output avg/max, '2.1 sec TTFT overage' (note the word), '61 sec p95 TTFT (1M ctx)', 81k avg input, 41% cache hit, '0 chat logs kept', 60×B300 '15 trays', bf16 attention + fp8 experts/KV, no MTP no Eagle, and the third-party cost estimate (whose post is that? paxaral) — with post URLs.
STAGE 2: read https://inference-margins.pages.dev/ §2c and every other place these numbers appear (§3 calibration table, §4, the GLM presets' notes, the ncode traffic profile '8:1 / 41%'). Verify EVERY transcription: exact numbers, the [sic] handling of 'overage', the p95 inclusion, units, hardware count, precision, the no-MTP claim, and that the page cleanly separates his deployment from the SGLang/NVIDIA DeepSeek-V4 record and Baseten's GLM-5.2 work. Also audit the derived uses: the 'ncode coding deployment 8:1 / 41%' traffic profile and the GLM preset's ioRatio/cacheHit — is deriving a reusable 'coding deployment' profile from his one-week free event a fair, labeled inference (81k-token inputs at 1M ctx; does 8:1 even follow from his posts, or from elsewhere)? Classify each representation with the five classes.
